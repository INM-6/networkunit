# readthedocs version
version: 2

build:
    os: ubuntu-22.04
    tools:
      python: "3.8"

sphinx:
  builder: html
  configuration: doc/conf.py

conda:
  environment: ./environment-docs.yml

python:
    install:
        - method: pip
          path: .
          extra_requirements:
              - docs